pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh '''
                echo "Hello from Jenkins CI/CD"
                '''
            }
        }
    }
}
